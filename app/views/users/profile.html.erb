<h1>profile</h1>
<div class="row">
  <div class="col-md-3">
    <h5><%= link_to "Add Place Rating", new_response_path, class: "btn btn-primary" %></h5>
  </div>
  <div class="col-md-3 col-md-offset-6">
    <div>
      <h5>Itineraries (link eventually)</h5>
    </div>
  </div>
</div>

<table class="table">
  <th>Places I've Rated</th>
  <th>My Rating</th>
    <% @my_places.each do |place| %>
      <tr>
        <td><%= place.name %></td>
        <td><%= place.place_rating(current_user) %></td>
        <td> Change Rating (button eventually) </td>
        <td></td>
        <td></td>
      </tr>
    <% end %>
  </table>
